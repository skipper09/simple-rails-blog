<h1>Sarah's Ruby Blog</h1>
<% @posts.each do |post| %>
  <div class="card">
    <div class="card-body">
      <h3><%= post.title %></h3>
      <p><%= post.body %></p>
      <%= link_to "Read More", post_path(post), :class => 'btn btn-primary' %>
    </div> 
  </div>
<% end %>
